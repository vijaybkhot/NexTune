query SearchArtistByArtistName($searchTerm: String!) {
  searchArtistByArtistName(searchTerm: $searchTerm) {
    _id
    name
    dateFormed
    members
    albums {
      _id
      title
    }
    numOfAlbums
  }
}
